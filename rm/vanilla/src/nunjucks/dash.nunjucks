{% macro dashComponent(components={}) %}

  {% for info in components %}
  <div class="component">
    {% if info.title %}
      <h2>{{info.title}}</h2>
    {% endif %}
    <template id="{{info.id}}" class="{{info.class}}">

    </template>
  </div>
  {% endfor %}

{% endmacro %}
